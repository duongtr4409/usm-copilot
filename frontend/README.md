USM Frontend (minimal scaffold)

Commands:

- Install dependencies: `npm install`
- Start dev server: `npm run dev`
- Run tests: `npm test`

Environment:
- `VITE_API_BASE` can be set to point to backend API base (default http://localhost:8080/api/v1)

MSW mock handlers run automatically in development.
